{
  "name": "ocr-workbench",
  "version": "4.0.0",
  "description": "Dual-engine OCR Workbench — Tesseract.js (browser) + Tencent Cloud OCR (serverless proxy)",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "deploy": "vercel deploy --prod"
  }
}
