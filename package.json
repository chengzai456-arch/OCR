{
  "name": "ocr-workbench",
  "version": "5.0.0",
  "description": "Pure browser-side OCR Workbench powered by Tesseract.js — zero server, zero API keys",
  "private": true,
  "scripts": {
    "dev": "npx serve ."
  }
}
