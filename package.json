{
  "name": "ocr-workbench",
  "version": "3.0.0",
  "description": "Browser-based OCR Workbench — Tesseract.js + PDF.js, no API keys, no backend",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "deploy": "vercel deploy --prod"
  }
}
